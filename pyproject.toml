[project]
name = "stat-test-props"
version = "0.1.0"
description = "Two-proportion z-test Streamlit app"
requires-python = ">=3.13"
dependencies = [
    "streamlit",
    "numpy",
    "scipy",
    "statsmodels",
]

[dependency-groups]
dev = [
    "black",
    "isort",
    "pylint",
    "pytest",
]

